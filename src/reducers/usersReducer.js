//updating the parts of our store that have to do with User

export default function usersReducer(state = {users: []}, action) {
    switch(action.type) {
        default:
            return state
    }

}